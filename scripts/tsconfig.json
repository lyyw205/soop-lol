{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  // 검증 스크립트도 실행되는 코드다. 여기가 검사에서 빠져 있으면
  // "검증이 통과했다"가 사실은 "검증 스크립트가 컴파일도 안 된다"일 수 있다.
  "include": ["**/*.ts"],
  "exclude": ["node_modules"]
}
